// Función tipo expresión o anónima
let x = function(a, b){return a + b}; // Necesita cierre con punto y coma
resultado = x(2, 4); // Se llama con la variable (x) y los paréntesis

console.log(resultado);